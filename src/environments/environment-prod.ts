export const environment = {
    production: true,
    apiUrl: 'API_URL', //This will send the request to the backendURL, stored as environment variable 
    apiKey: 'API_KEY'    //This will get the api key stored as an environment variable
};